This is a transnational data set which contains all the transactions occurring between 01/12/2010 and 09/12/2011 for a UK-based and registered non-store online retail.
The company mainly sells unique all-occasion gifts. Many customers of the company are wholesalers.

InvoiceNo: Invoice number. Nominal, a 6-digit integral number uniquely assigned to each transaction. If this code starts with letter 'c', it indicates a cancellation.
StockCode: Product (item) code. Nominal, a 5-digit integral number uniquely assigned to each distinct product.
Description: Product (item) name. Nominal.
Quantity: The quantities of each product (item) per transaction. Numeric.
InvoiceDate: Invice Date and time. Numeric, the day and time when each transaction was generated.
UnitPrice: Unit price. Numeric, Product price per unit in sterling.
CustomerID: Customer number. Nominal, a 5-digit integral number uniquely assigned to each customer.
Country: Country name. Nominal, the name of the country where each customer resides.

InvoiceNo（发票编号）：标称型，一个由6位整数组成的编号，唯一分配给每个交易。如果该编号以字母“C”开头，则表示该交易为取消。

StockCode（产品代码）：标称型，一个由5位整数组成的代码，唯一分配给每个不同的产品。

Description（产品名称）：标称型，表示产品（物品）的名称。

Quantity（数量）：数值型，表示每次交易中每种产品（物品）的数量。

InvoiceDate（发票日期）：数值型，表示每次交易生成的日期和时间。

UnitPrice（单价）：数值型，表示每个产品的单价（以英镑计）。

CustomerID（客户编号）：标称型，一个由5位整数组成的编号，唯一分配给每个客户。

Country（国家）：标称型，表示每个客户所在的国家名称。